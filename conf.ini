[files]
set_defs = set_defs.csv

[format]
read_format = deckstats
write_format = excel
fields_deckstats = amount, card_name, is_foil, is_pinned, set_id
fields_output = NAME, QTY, LOW (ea.), MID (ea.), HI (ea.), LOW, MID, HI
